# healstack

Install the public SDK from npm:

```bash
npm install healstack
```

Quick example:

```javascript
import HealStack from "healstack";

HealStack.init({ apiKey: "demo-key", autoHeal: true });
```

For Beta release details, see the project's release notes: `../docs/release-notes-beta.md`.